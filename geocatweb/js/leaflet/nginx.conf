
user  nobody;
#user  root;
worker_processes  1;

error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  debug;

#pid        logs/nginx.pid;


events {
    worker_connections  1024;
	
}


http {
    
	 root  /usr/local/nginx/html;	
	include       mime.types;
    default_type  application/octet-stream;
	client_max_body_size 20M;

	#gzip on;
	#gzip_disable "MSIE [1-6].(?!.*SV1)";
	#gzip_comp_level 6;
	#gzip_proxied any;
	#gzip_buffers 4 32k;
	#gzip_buffers 16 8k;
	#gzip_types  text/plain text/css application/json application/x-javascript text/xml application/xml application/xml+rss text/javascript image/x-icon image/bmp;
	#gzip_vary on;
	
   # log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                '$status $body_bytes_sent "$http_referer" '
     #                '"$http_user_agent" "$http_x_forwarded_for" "$fastcgi_script_name"';

   # access_log  logs/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;

    gzip  on;
	#gzip on;
	#gzip_proxied any;
	#gzip_types text/plain text/xml text/css application/x-javascript;
	#gzip_vary on;
	#gzip_disable "msie6″;
	
	
	#server {
#		server_name  instamapes.icgc.cat;
#		return 301 $scheme://www.instamaps.cat$request_uri;
#	}

	
	server {
        listen       80;
        server_name  localhost;
		client_max_body_size 20M;
		#error_log logs/error.log;
		#error_log  logs/error.log  debug;
        #charset koi8-r;

        #access_log  logs/host.access.log  main;

		location / {
			if (-f $document_root/manteniment.html) {
                return 503;
			}
			
			root   /usr/local/nginx/html;	
			index  index.html index.htm;
			client_max_body_size 20M;
			#if ($request_method = 'OPTIONS') {
				add_header 'Access-Control-Allow-Origin' '*';
				add_header 'Access-Control-Allow-Credentials' 'true';
				 add_header 'Access-Control-Allow-Headers' 'Content-Type,Accept';
				add_header 'Access-Control-Allow-Methods' 'POST, GET, OPTIONS';
				add_header 'Access-Control-Max-Age' '1728000';
				#add_header 'Content-Type' 'text/plain; charset=UTF-8';
				#return 200;
			#}
		}
		
		# error 503 redirect to manteniment.html
        error_page 503 @maintenance;
        location @maintenance {
              rewrite ^(.*)$ /manteniment.html break;
        }
		
			
        #location / {
        #    root   /usr/local/nginx/html;	
        #    index  index.html index.htm;	
			
        #}

        #error_page  404              /404.html;

        # redirect server error pages to the static page /50x.html
        #
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }

		
	#location ~ \.fcgi$ {
			#root           html;
           # fastcgi_pass   127.0.0.1:9001;
		  # fastcgi_pass unix:/var/run/fcgiwrap.socket;
          # fastcgi_index  mapserv.fcgi;
          # fastcgi_param  SCRIPT_FILENAME  /usr/local/nginx/service/$fastcgi_script_name;
           # include        fastcgi_params;
       # }	 
	

	
		#proxt ro geocat02
	location /geocat/ {
			 proxy_pass http://172.70.1.12;
			  proxy_read_timeout 120s;
		}
	
	
		location /share/ {
					proxy_pass http://172.70.1.12;
					 proxy_read_timeout 120s;
				}


	
		#MapServer
		location /maps/ {
			gzip off;
			fastcgi_pass unix:/var/run/fcgiwrap.socket;
			
			include fastcgi_params;
			fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
		}
	
	
	
	
	
	
	
	#Mapcache
	 location ~ ^/mapcache(?<path_info>/.*|$) {	
	# location /mapcache/ {
 root  /usr/local/nginx/html;		 
 gzip off;
           set $url_prefix "/mapcache";
           mapcache /opt/geocat/dades/mapcache/mapcache.xml;
		  
		 add_header 'Access-Control-Allow-Origin' '*';
				add_header 'Access-Control-Allow-Credentials' 'true';
				 add_header 'Access-Control-Allow-Headers' 'Content-Type,Accept';
				add_header 'Access-Control-Allow-Methods' 'POST, GET, OPTIONS';
				add_header 'Access-Control-Max-Age' '1728000';
		  
		  
          error_page 404 = @fcgi_mapcache;
		    
    
		  
        }

	#MapCache 404
     location @fcgi_mapcache {

	 proxy_pass http://127.0.0.1:8080;
	 

	 
   #fastcgi_param  QUERY_STRING     $query_string;
   #fastcgi_param  REQUEST_METHOD   $request_method;
   #fastcgi_param  CONTENT_TYPE     $content_type;
   #fastcgi_param  CONTENT_LENGTH   $content_length;
   #fastcgi_param  PATH_INFO        $path_info;
   #fastcgi_param  SERVER_NAME      $server_name;
   #fastcgi_param  SERVER_PORT      $server_port;
   #fastcgi_param  SCRIPT_NAME      "/mapcache";
	 #fastcgi_pass   unix:/var/run/fcgiwrap.socket; 
	
		 
      }
	
		
        # proxy the PHP scripts to Apache listening on 127.0.0.1:80
        #
        #location ~ \.php$ {
        #    proxy_pass   http://127.0.0.1;
        #}

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        #
        #location ~ \.php$ {
        #    root           html;
        #    fastcgi_pass   127.0.0.1:9000;
        #    fastcgi_index  index.php;
        #    fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
        #    include        fastcgi_params;
        #}

        # deny access to .htaccess files, if Apache's document root
        # concurs with nginx's one
        #
        #location ~ /\.ht {
        #    deny  all;
        #}
    }


    # another virtual host using mix of IP-, name-, and port-based configuration
    #
    #server {
    #    listen       8000;
    #    listen       somename:8080;
    #    server_name  somename  alias  another.alias;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}


    # HTTPS server
    #
    #server {
    #    listen       443 ssl;
    #    server_name  localhost;

    #    ssl_certificate      cert.pem;
    #    ssl_certificate_key  cert.key;

    #    ssl_session_cache    shared:SSL:1m;
    #    ssl_session_timeout  5m;

    #    ssl_ciphers  HIGH:!aNULL:!MD5;
    #    ssl_prefer_server_ciphers  on;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}

}
